replicaCount: 2

image:
  repository: sebastianjordan19/echo-native
  tag: "1.0.0"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080
  targetPort: 8080

resources:
  limits:
    memory: 1Gi
  requests:
    memory: 1Gi

ingress:
  enabled: false
